{
  "name": "codex-exec-remote",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "bin": {
    "codex-exec-remote": "./src/index.ts"
  },
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun test",
    "build": "bun build --compile src/index.ts --outfile codex-exec-remote",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/ws": "^8.18.1",
    "typescript": "^5.9.3"
  }
}
